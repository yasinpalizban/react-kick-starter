import {ISetting} from "./setting.interface";

export interface IHomeSetting {
    twitter?: ISetting,
    instagram?: ISetting,
    googlePlus?: ISetting
    facebook?: ISetting,
    address?: ISetting,
    phone?: ISetting,
    email?: ISetting,
    latitude?: ISetting,
    longitude?: ISetting,
    linkLocation?: ISetting,
}

